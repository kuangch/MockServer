/* ========================================
 *  company : Dilusense
 *   author : Kuangch
 *     date : 2018/11/22
 * ======================================== */

let path = require("path");
let Mock = require(require('app-root-path') + path.sep + 'mock' + path.sep + 'mock');

module.exports = {
    Mock
}